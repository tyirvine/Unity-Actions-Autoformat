#!/bin/sh -l

# Setup variables
path=$1
token=$2

# Confirm existence of folder
if [ -d $path ]; then
    echo "$path exists"

else
    echo "$path does not exist"

# Install dotnet format

# Format files in folder

# Check for changes

# Commit

# Push


fi